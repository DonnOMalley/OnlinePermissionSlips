@model IEnumerable<OnlinePermissionSlips.Models.DAL.SystemConfiguration>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EmailDomain)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmailAPIKey)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DefaultFromAddress)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EmailDomain)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmailAPIKey)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DefaultFromAddress)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
